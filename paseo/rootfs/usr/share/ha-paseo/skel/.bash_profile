# Seeded by the Paseo add-on; never overwritten. Edit freely.
#
# Paseo terminal panes start a LOGIN shell, which reads this file and not
# ~/.bashrc. Without this line none of the interactive setup would apply.

# shellcheck source=/dev/null
[[ -f ~/.bashrc ]] && . ~/.bashrc
